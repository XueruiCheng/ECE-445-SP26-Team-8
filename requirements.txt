# Facial Quantum Matching Mirror — Python Dependencies
# Install with: pip install -r requirements.txt
# On Raspberry Pi: pip3 install -r requirements.txt --break-system-packages

# ── Core (all platforms) ──────────────────────
# Camera and image processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Face detection and embedding
onnxruntime>=1.17.0

# Math and data
numpy>=1.24.0
scipy>=1.11.0

# Display
pygame>=2.5.0

# Testing
pytest>=7.0.0

# ── Pi only (will fail on macOS/Windows — see README) ──
# Uncomment on the Raspberry Pi:
# PyBluez==0.23